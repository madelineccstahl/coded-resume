//WRITE YOUR CODE BELOW
var customerDrink = {
    name: "Pumpkin Spice",
    sugarcont: 100,
    ready: true,
}

console.log(customerDrink.name);
console.log(customerDrink.sugarcont);

if(customerDrink.ready) {
    console.log("ready for pickup");
} else {
    console.log("still in order queue");
}